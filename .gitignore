node_modules/
.next/
.DS_Store
.vercel/
.turbo/
*.env.local
*.tsbuildinfo
.ai-usage/
.claude/*
!.claude/settings.json
!.claude/hooks/

# Second brain (tracked separately via 'brain' remote)
CLAUDE.md
.brain/

# Nested git repos — register as submodules via terminal (git submodule add)
port/
ops-dashboard/

# Wrangler local state
.wrangler/

# Sensitive env files (created by Vercel CLI or manual — never commit)
.env.port-check
*.env.production
*.env.vercel

{
  "name": "@windedvertigo/stripe",
  "version": "0.1.0",
  "private": true,
  "description": "Shared harbour Stripe utilities — client, checkout, webhook handling, and entitlement granting for cross-app commerce.",
  "scripts": {
    "build": "echo 'stripe: no build step'"
  },
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./webhook": "./webhook.ts"
  },
  "files": [
    "index.ts",
    "client.ts",
    "checkout.ts",
    "webhook.ts",
    "queries.ts"
  ],
  "peerDependencies": {
    "@neondatabase/serverless": ">=0.9.0",
    "stripe": ">=14"
  }
}

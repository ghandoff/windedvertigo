{
  "name": "@harbour/values-auction-relay",
  "private": true,
  "version": "0.1.0",
  "description": "ws relay (cloudflare worker + durable object) for values-auction cross-device sync.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.114.0"
  }
}

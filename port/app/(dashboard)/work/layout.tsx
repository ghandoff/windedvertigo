// WorkModeNav removed — contracts and studios moved to /projects.
// Only /work/time remains as a child route.

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

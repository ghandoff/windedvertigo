{
  "name": "@windedvertigo/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Auth.js v5 configuration — Google SSO with domain + allowlist gating for all winded.vertigo apps.",
  "scripts": {
    "build": "echo 'auth: no build step'"
  },
  "main": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "files": [
    "*.ts"
  ],
  "peerDependencies": {
    "next": ">=16",
    "next-auth": ">=5.0.0-beta.25",
    "@neondatabase/serverless": ">=0.10.0"
  }
}

# Live state

> Single-writer file. Owned by the `context-sync` scheduled task (Cowork, 9pm PT daily). Manual edits should only happen when explicitly taking the snapshot; otherwise let the schedule rewrite it.
>
> History and per-session notes live in sibling files in this directory. The archive of everything pre-split is `_archive-pre-split-2026-05-19.md`.

**last synced:** 2026-05-19 ~21:50 PT (claude-code, manual — at moment of split)

## where we are right now

The single-file `handoff.md` was split into a directory of per-session files at 2026-05-19 to eliminate the recurring merge-conflict pattern (14 edits / 30d by ≥2 concurrent sessions). Until the next `context-sync` run, this `_live-state.md` is a placeholder pointing at the archive — read `_archive-pre-split-2026-05-19.md` for the full freshest snapshot prior to the split.

## open threads

See `_archive-pre-split-2026-05-19.md` § *open threads* until `context-sync` re-runs and rewrites this section.

## environment handoffs

See `_archive-pre-split-2026-05-19.md` § *environment handoffs* until `context-sync` re-runs.

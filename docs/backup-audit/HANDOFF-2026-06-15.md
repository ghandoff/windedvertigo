# Handoff — backup audit + desktop→laptop transition (2026-06-15)

Written as Garrett moved from the office desktop to his laptop. Everything below is
pushed to `origin` (github.com/ghandoff/windedvertigo) **except** where noted as
local-only. The laptop gets everything on `origin` via `git fetch`.

## ✅ Done this session (all on origin unless noted)
- **wv-ppcs-impact recovered.** The live Worker had no source anywhere. Pulled the deployed
  bundle from Cloudflare → `apps/ppcs-impact/dist/index.js` + `RECOVERED-FROM-CLOUDFLARE.md`,
  pushed on branch **`recover/ppcs-impact-from-cloudflare`**. (Bundle only — original `src/`,
  static ASSETS, and real `wrangler.jsonc` still need recovery from the deploying machine.)
- **All work-at-risk branches backed up.** Pushed every local branch with unpushed commits.
  Four had *diverged* from their remotes (can't fast-forward without force) — their local tips
  are preserved as dated refs so nothing is lost; reconcile later:
  - `save/update-tasks-md-localtip-20260615`
  - `save/self-running-auction-game-7CbZz-localtip-20260615`
  - `save/booking-package-e4-localtip-20260615`
  - `save/proposal-pipeline-cron-and-consumer-localtip-20260615`
- **whirlpool-wheel WIP** (was untracked on `main`) → `save/whirlpool-wheel-wip-20260615`.
- **This audit** → `docs/backup-audit/findings-2026-06-15.md` (+ this file).

## 🚧 Open items (decide / do later — NOT done)

### 1. feat/caipb-viz — LOCAL-ONLY, holds a large live-credential set. DO NOT push as-is.
This branch (3 commits of caipb/AICS dashboard work) is **only on the desktop** — it was never
pushed, and GitHub push-protection actively rejects it. It contains **two real credential
backup files** committed because this branch predates the `port/.gitignore` rule that protects them:
- `port/.env.local.bak.20260527T055218`
- `port/.env.local.pre-restore-clobber`

GitHub flagged **8+ live secrets** in them: Vercel API Key, Google OAuth (Refresh Token + Client
ID + Client Secret), LinkedIn Client Secret, Cloudflare API token, **+3 more** (also seen in the
files: Anthropic, OpenAI, Notion, Slack, Resend, Vercel OIDC, CF account id).

**Status:** contained (never left this machine). The branch was restored to its exact original
state — no partial rewrite left behind.
**To resume (dedicated session, NOT mid-transition):**
1. `git filter-repo` (or filter-branch) over the branch to remove all three secret files
   (`site/.env`, both `port/.env.local.*`) from history.
2. Verify with a dry-run push (push-protection must pass clean) before any real push.
3. **Rotate the exposed credential set** — they sat in local git history. The caipb work itself
   is worth keeping; the secrets are the blocker.
4. Because this branch is desktop-only, the caipb-viz **work will not be on the laptop** until
   step 1–2 are done and it's pushed.

### 2. Cloudflare API token rotation — dashboard-gated
The CF token found in `site/.env` (on feat/caipb-viz) is **active**, id
`0e51aecfc0671dfb55d8f456be2b234b`, no expiry. It's the **CI deploy token** (used by
`.github/workflows/deploy-nordic.yml` and `deploy-values-auction.yml`). Local wrangler uses
OAuth, so interactive deploys are unaffected by a roll.
- I **cannot roll it from here** — rolling an API token is a Cloudflare dashboard action (or needs
  an "API Tokens: Edit" credential); the CF MCP only manages Workers/KV/R2/D1.
- **To do (2 min, from the laptop):** CF dashboard → My Profile → API Tokens → find token id
  `0e51aecf…` → Roll. Then update the GitHub Actions secret: `gh secret set CLOUDFLARE_API_TOKEN`
  for `ghandoff/windedvertigo` (I can do that step once you have the new value).

### 3. amy-messages — still unbacked (left per your call)
`~/Projects/amy-messages/` is a live Worker (deployed 2026-06-02) but **not a git repo** and no
GitHub repo exists. When ready: `git init` there + create `ghandoff/amy-messages` (private) + push.
`.gitignore` already shields the 2.9 GB SMS xml.

### 4. Orphan / stub Workers (cleanup, no backup risk)
Confirm unused then delete: `wv-port-production` (empty stub), `paper-trail-production`,
`wv-harbour-feel-cards` + `wv-feel-cards-relay` (renamed → read-the-room 2026-05-06).

### 5. .gitignore hardening (the Nordic / caipb pattern)
Add a bare `.env` rule (with `!.env.example`) to root `.gitignore` and `port/.gitignore` — both
currently use `.env*.local`, which does NOT catch `.env`, `.env.local.bak.*`, or
`.env.local.pre-restore-clobber`. This is exactly how the caipb-viz secrets slipped in.

## Resume on the laptop
```sh
cd ~/Projects/windedvertigo && git fetch origin --prune
# new branches available: recover/ppcs-impact-from-cloudflare, save/*-localtip-20260615,
# save/whirlpool-wheel-wip-20260615, chore/backup-audit-2026-06-15
git checkout chore/backup-audit-2026-06-15   # this handoff + the full findings
```
The full backup-status table and guardrail recommendations are in
`docs/backup-audit/findings-2026-06-15.md`.

# Opsy handoff — 2026-06-30

Snapshot for the next conversation. Legend: ✅ done · ⏳ fixed, self-validating · 🔧 needs your action · 🟠 open decision.

---

## 1. Security — RLS ✅ CLOSED
Every data-bearing Supabase project now has RLS on all public tables **plus** a `force_rls_on_new_tables_trg` event trigger so new tables can't drift open again:

| Project | ID | Status |
|---|---|---|
| wv-port-pilot | `fpqbokzjipovjhvujqtm` | 0 unprotected ✅ |
| wv-booking | `phvmhjtfxyvhjfjnlalg` | locked 06-26 ✅ |
| PPCS (labelled "wv-nordic") | `nzdfpfrnilreqzmthpui` | 5 tables fixed 06-29 ✅ |

The 06-23 "two projects" critical is fully remediated; resolves posted to #ops-alerts. **The Supabase MCP is connected** (`execute_sql` / `apply_migration` / `get_advisors`), so Opsy can now audit and apply RLS directly.

## 2. Open incidents ⏳ (fixed, awaiting next run)
- **rfp-gmail-scanner** — you added `gmail.modify` to SA client `112925…`. Validates on the next **08:00 UTC** cron run.
- **meet-transcript-ingest** — `GOOGLE_IMPERSONATE_SUBJECTS=garrett@windedvertigo.com` deployed to wv-port. Validates on the next **hourly** run. Transcripts confirmed present in your "Meet Recordings" folder. To sweep the backlog: `GET /api/cron/meet-transcript-ingest?sinceDays=10` with the cron bearer token.
- The daily triage will post the #ops-alerts resolves when each goes green. No action needed.

## 3. Automated triage ✅ / 🔧
`opsy-daily-triage` scheduled task created (daily **08:37 local**) — loads the briefing, triages open incidents, commits code fixes, escalates secret/admin/DB items, posts threaded resolves, stays quiet when green. **🔧 Hit "Run now" once** in the Scheduled sidebar to pre-approve its tools.

## 4. Dependency review (06-29)
- ✅ **vitest CRITICAL CVE** — fixed + committed in harbour-apps (`f9302f9`), critical → 0.
- ✅ **wrangler → 4.105.0** — committed both repos (wv `e0e28b9d`, harbour-apps `0ded53e`); HIGH findings 9 → 5 each.
- 🟠 **Majors (assessed, not bumped — all break live code):** `ai` 6→7 + `@ai-sdk/anthropic` 3→4 (port/wv-claw, HIGH), `@anthropic-ai/sdk` drift in harbour-apps (MEDIUM), `stripe` 17→22 in 9 apps (MEDIUM). Recommended order: **stripe + anthropic-sdk first, AI SDK 7 last & alone.** Full per-item plan → `docs/opsy/dependency-majors-and-credentials.md`.

## 5. Monitoring blind spots 🔧
Six checks are locked awaiting credentials — add as **wv-port** secrets (least-privilege/read-only). Priority first two:
`CLOUDFLARE_API_TOKEN` (also unblocks the wv-site KV fix), `STRIPE_SECRET_KEY`, then `SUPABASE_NORDIC_URL` (=`https://nzdfpfrnilreqzmthpui.supabase.co`) + `SUPABASE_NORDIC_SECRET_KEY`, `VERCEL_API_TOKEN`, `GITHUB_TOKEN`, `NEON_API_KEY`. Full table → same doc as above.

## 6. Still open 🟠
- **generate-pdfs cron (500)** — `@react-pdf/renderer` needs Node and can't run on workerd; port is on CF Workers now. **Needs a runtime decision:** run that one route on a Node/Vercel function, or swap to a workerd-compatible PDF path. Not yet actioned.

## 7. Performance (known, mitigated)
wv-site/creaseworks spikes: `force-dynamic → revalidate=300` deployed ~06-23, spikes stopped. Durable fix still wants `CLOUDFLARE_API_TOKEN` so `NEXT_INC_CACHE_KV` populates at deploy. supabase-pilot alert threshold raised to 1500ms (was noise at 1000ms).

---

### What's next, shortest path
1. **You:** add `CLOUDFLARE_API_TOKEN` + `STRIPE_SECRET_KEY` to wv-port (biggest visibility win, unblocks wv-site KV).
2. **You:** "Run now" on `opsy-daily-triage` to pre-approve tools.
3. **Schedule:** the 3 dependency majors per the plan doc (stripe + anthropic-sdk first).
4. **Decide:** generate-pdfs runtime.
5. **Auto:** rfp-gmail-scanner + meet-transcript-ingest resolve themselves on next run; triage posts the confirmations.

### Two hard lessons banked
- Uncommitted working-tree edits get wiped by branch switches — commit/install promptly (this ate a vitest edit).
- `npm install --package-lock-only` won't force-upgrade an already-locked dep; use explicit `npm install pkg@ver -w <workspace>`.
